spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#          uri: file:///YOUR_PATH//microservices-lab/config-server-repository
          uri: https://github.com/berkanterdogan/microservices-lab-spring-cloud-config-repository
          default-label: ${SPRING_CLOUD_CONFIG_SERVER_GIT_DEFAULT_LABEL:master}
          username: ${SPRING_CLOUD_CONFIG_SERVER_GIT_USERNAME}
          password: ${SPRING_CLOUD_CONFIG_SERVER_GIT_PASSWORD}
          clone-on-start: true
      fail-fast: true
  security:
    user:
      name: ${SPRING_SECURITY_USER_NAME:config_server_user}
      password: ${SPRING_SECURITY_USER_PASSWORD:ENC(TiXQa2AbV2dkQx/UjiAWT4chFdhy5AJ74O0UuUi/UHsZL6OiSUnne06lx5kgAtlyc/dqSjJbPhMDb94+Tqd+YQ==)}

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD:jspypt_Pwd351} # Remove the default value for real production applications! Set it as an environment variable.