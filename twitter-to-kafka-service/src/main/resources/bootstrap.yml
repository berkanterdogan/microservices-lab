spring:
  application:
    name: twitter-to-kafka-service
  profiles:
    active: twitter_to_kafka_service
  cloud:
    config:
      name: twitter-to-kafka-service, config-client
      username: ${SPRING_CLOUD_CONFIG_USERNAME:config_server_user}
      password: ${SPRING_CLOUD_CONFIG_PASSWORD:ENC(TiXQa2AbV2dkQx/UjiAWT4chFdhy5AJ74O0UuUi/UHsZL6OiSUnne06lx5kgAtlyc/dqSjJbPhMDb94+Tqd+YQ==)}
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888} # It's default value.

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD:jspypt_Pwd351!} # Remove the default value for real production applications! Set it as an environment variable.
